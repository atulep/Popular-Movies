<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.boisestate.azamattulepbergenovu.popularmovies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailActivity"></activity>
        <!-- Not sure why to include the exported property and why name contains generated -->
        <provider
            android:authorities="edu.boisestate.azamattulepbergenovu.popularmovies.Data.MoviesProvider"
            android:name="edu.boisestate.azamattulepbergenovu.popularmovies.Data.generated.MoviesProvider"
            android:exported="false"/>
    </application>

</manifest>
